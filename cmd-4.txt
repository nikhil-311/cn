BDA Experiment 4 — Titanic Data Analysis using MapReduce
(Complete Step-by-Step in REAL Hadoop)
From your manual: 

What This Practical Does
We analyze Titanic passenger data to find:
1. Average age of male passengers who died 
2. Number of female passengers who died in each class 

STEP 1 — Open Ubuntu Terminal
Shortcut:
Ctrl + Alt + T

STEP 2 — Start Hadoop Services
Run:
start-dfs.sh
Then:
start-yarn.sh

STEP 3 — Verify Hadoop Running
Run:
jps
Expected:
NameNode
DataNode
SecondaryNameNode
ResourceManager
NodeManager

STEP 4 — Create Project Folder
Run:
mkdir titanic_analysis
Go inside folder:
cd titanic_analysis

STEP 5 — Create titanic.csv
Run:
nano titanic.csv
Paste this sample data from manual: 
PassengerId,Survived,Pclass,Name,Sex,Age
1,0,3,John,male,22
2,1,1,Mary,female,38
3,1,3,Anna,female,26
4,0,1,Bob,male,35
5,0,2,Lily,female,30
6,0,3,Susan,female,18
Save:
* CTRL + O 
* Enter 
* CTRL + X 

STEP 6 — Create mapper.py
Run:
nano mapper.py
Paste:
#!/usr/bin/env python3

import sys

for line in sys.stdin:

    if line.startswith("PassengerId"):
        continue

    fields = line.strip().split(',')

    if len(fields) < 6:
        continue

    survived = fields[1]
    pclass = fields[2]
    sex = fields[4].lower()
    age = fields[5]

    if survived == '0':

        if sex == 'male' and age:
            print("male\t" + age)

        elif sex == 'female':
            print(f"class_{pclass}\t1")
Save:
* CTRL + O 
* Enter 
* CTRL + X 

STEP 7 — Create reducer.py
Run:
nano reducer.py
Paste:
#!/usr/bin/env python3

import sys

male_age_sum = 0
male_count = 0

class_deaths = {}

for line in sys.stdin:

    key, value = line.strip().split('\t')

    if key == "male":

        try:
            age = float(value)

            male_age_sum += age
            male_count += 1

        except ValueError:
            continue

    elif key.startswith("class_"):

        if key in class_deaths:
            class_deaths[key] += 1

        else:
            class_deaths[key] = 1

if male_count > 0:

    avg_age = male_age_sum / male_count

    print(f"Average age of deceased males: {avg_age:.2f}")

for cls in sorted(class_deaths):

    print(f"{cls.replace('class_', 'Class ')} female deaths: {class_deaths[cls]}")
Save:
* CTRL + O 
* Enter 
* CTRL + X 

STEP 8 — Give Execution Permission
Run:
chmod +x mapper.py
chmod +x reducer.py

STEP 9 — Create HDFS Input Folder
Run:
hdfs dfs -mkdir /titanic_input

STEP 10 — Upload CSV File to HDFS
Run:
hdfs dfs -put titanic.csv /titanic_input

STEP 11 — Verify Upload
Run:
hdfs dfs -ls /titanic_input
Expected:
titanic.csv

STEP 12 — Run Hadoop MapReduce Job
MOST IMPORTANT COMMAND:
hadoop jar /usr/lib/hadoop-mapreduce/hadoop-streaming.jar \
-files mapper.py,reducer.py \
-mapper mapper.py \
-reducer reducer.py \
-input /titanic_input/titanic.csv \
-output /titanic_output
Wait until Hadoop finishes.

STEP 13 — View Final Output
Run:
hdfs dfs -cat /titanic_output/part-00000

EXPECTED OUTPUT
From manual: 
Average age of deceased males: 28.50
Class 2 female deaths: 1
Class 3 female deaths: 1

IMPORTANT ERROR FIX
If Output Folder Already Exists
Run:
hdfs dfs -rm -r /titanic_output
Then rerun STEP 12.

SIMPLE UNDERSTANDING
Mapper Does
Filters:
* males who died 
* females who died 
and sends:
* ages 
* class counts 

Reducer Does
1. Calculates: 
average male age
2. Counts: 
female deaths per class

Important Viva Questions
Why Titanic dataset?
Titanic dataset is commonly used for data analysis and filtering operations.

What does Mapper do?
Mapper filters deceased male and female passengers from the dataset.

What does Reducer do?
Reducer calculates average age and counts female deaths by class.

What is aggregation?
Aggregation means combining multiple values to produce summarized results.

Important Commands To Remember
PurposeCommandUpload filehdfs dfs -putRun MapReducehadoop jar ...View outputhdfs dfs -catDelete outputhdfs dfs -rm -r
Journal Conclusion
Thus, Titanic passenger data was successfully analyzed using MapReduce in Hadoop to calculate average male age and female deaths by class.

